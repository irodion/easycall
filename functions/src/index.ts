// EasyCall Cloud Functions (v2)
//
// Future functions to implement:
// - onIncomingCall: Firestore trigger on users/{userId}/incomingCall — sends FCM push notification
// - validatePairingCode: HTTPS callable — validates pairing code and creates caregiver link
// - generateJitsiJwt: HTTPS callable — generates JaaS JWT for authenticated Jitsi sessions
// - cleanupExpiredPairingCodes: Scheduled — removes expired pairing codes

export {};
